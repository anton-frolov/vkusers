create table user_info(
    user_id bigint primary key,
    user_f_name varchar(256),
    user_l_name varchar(256),
    user_b_date timestamp,
    user_city varchar(256),
    user_contacts varchar(256)
);